// `Use cards view for showing the movies list.`
// `Route should be /movies-card, show 10 movies by default in descending by date`
// `Use infinite scrolling for getting more data `
// `Need to add filters and sort functionalities `
// `Search input for cards & Add Movie button as well on top of cards`
// `Details page on route /movies-card/:movie-id `
// `CRUD with separate routes. E.x => 1) /movies-card/create 2) /movies-card/:movie-id/edit (form component should be common)`
`Show a sample images carousel on movies detail page at top. `
// `Show all the fields with details in proper layout `
// `redirect to details page on clicking the card.`
// `redirect to edit page on edit button click`
// `Use same form component to create & edit`
// `delete movie on delete button click after confirmation.`


`Person module:`
// `Use List(Grid/Table) view for showing person list.`
// `CRUD with all the functionalities similar to Movies-Card module in Table.` 
// `Show 10 records by default and use pagination for showing more data `
// `Routes should be /person-list (For CRUD also) `
// `Search input for table & Add Person button as well on top of table` 
// `In action column view, edit & delete button ` 
// `/person-list/:person-id/edit for editing movie` 
// `/person/create for creating the movie `
// `Use same form component to create & edit similar to movies module.(Use form in ANTD modal component)`
` remaining`
// `Column view `
// `It will be person listing table. `